var robot = require('robotjs');

/* Configurar con la camara lo más alta posible, mirando al norte, zoom al máximo */

function dropInventory() {
    /* drop1();
    drop2(); */
    drop3();
    drop4();
    drop5();
    drop6();
    drop7();
    drop8();
    drop9();
    drop10();
    drop11();
    drop12();
    drop13();
    drop14();
    drop15();
    drop16();
    drop17();
    drop18();
    drop19();
    drop20();
    drop21();
    drop22();
    drop23();
    drop24();
    drop25();
    drop26();
    drop27();
    drop28();
}

function drop1() {
    var x = getRandomInt(1394, 1456);
    var y = getRandomInt(458, 515);
    robot.moveMouseSmooth(x, y, 1);
    robot.keyToggle('shift', 'down');
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop2() {
    var x = getRandomInt(1498, 1560);
    var y = getRandomInt(458, 515);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop3() {
    var x = getRandomInt(1598, 1660);
    var y = getRandomInt(458, 515);
    robot.keyToggle('shift', 'down');
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop4() {
    var x = getRandomInt(1704, 1766);
    var y = getRandomInt(458, 515);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop5() {
    var x = getRandomInt(1394, 1456);
    var y = getRandomInt(530, 587);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop6() {
    var x = getRandomInt(1498, 1560);
    var y = getRandomInt(530, 587);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop7() {
    var x = getRandomInt(1598, 1660);
    var y = getRandomInt(530, 587);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop8() {
    var x = getRandomInt(1704, 1766);
    var y = getRandomInt(530, 587);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop9() {
    var x = getRandomInt(1394, 1456);
    var y = getRandomInt(630, 656);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop10() {
    var x = getRandomInt(1498, 1560);
    var y = getRandomInt(630, 656);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop11() {
    var x = getRandomInt(1598, 1660);
    var y = getRandomInt(630, 656);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop12() {
    var x = getRandomInt(1704, 1766);
    var y = getRandomInt(630, 656);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop13() {
    var x = getRandomInt(1394, 1456);
    var y = getRandomInt(675, 732);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop14() {
    var x = getRandomInt(1498, 1560);
    var y = getRandomInt(675, 732);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop15() {
    var x = getRandomInt(1598, 1660);
    var y = getRandomInt(675, 732);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop16() {
    var x = getRandomInt(1704, 1766);
    var y = getRandomInt(675, 732);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop17() {
    var x = getRandomInt(1394, 1456);
    var y = getRandomInt(744, 801);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop18() {
    var x = getRandomInt(1498, 1560);
    var y = getRandomInt(744, 801);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop19() {
    var x = getRandomInt(1598, 1660);
    var y = getRandomInt(744, 801);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop20() {
    var x = getRandomInt(1704, 1766);
    var y = getRandomInt(744, 801);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop21() {
    var x = getRandomInt(1400, 1450);
    var y = getRandomInt(830, 870);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop22() {
    var x = getRandomInt(1498, 1560);
    var y = getRandomInt(830, 870);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop23() {
    var x = getRandomInt(1598, 1660);
    var y = getRandomInt(819, 876);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop24() {
    var x = getRandomInt(1704, 1766);
    var y = getRandomInt(830, 870);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop25() {
    var x = getRandomInt(1394, 1456);
    var y = getRandomInt(910, 946);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop26() {
    var x = getRandomInt(1498, 1560);
    var y = getRandomInt(910, 946);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop27() {
    var x = getRandomInt(1598, 1660);
    var y = getRandomInt(910, 946);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
}

function drop28() {
    var x = getRandomInt(1704, 1766);
    var y = getRandomInt(910, 946);
    robot.moveMouseSmooth(x, y, 1);
    sleep(getRandomInt(50, 100));
    robot.mouseClick();
    sleep(getRandomInt(500, 600));
    robot.keyToggle('shift', 'up');
}

function findTarget() {
    var xA = 590,
        yA = 330,
        widthA = 110,
        heightA = 100;
    var imgA = robot.screen.capture(xA, yA, widthA, heightA);

    var xB = 405,
        yB = 251,
        widthB = 135,
        heightB = 80;
    var imgB = robot.screen.capture(xB, yB, widthB, heightB);

    var xC = 705,
        yC = 435,
        widthC = 145,
        heightC = 25;
    var imgC = robot.screen.capture(xC, yC, widthC, heightC);

    var xD = 795,
        yD = 460,
        widthD = 55,
        heightD = 40;
    var imgD = robot.screen.capture(xD, yD, widthD, heightD);

    for (var i = 0; i < 10000; i++) {
        var random_xA = getRandomInt(0, widthA - 1);
        var random_yA = getRandomInt(0, heightA - 1);
        var random_xB = getRandomInt(0, widthB - 1);
        var random_yB = getRandomInt(0, heightB - 1);
        var random_xC = getRandomInt(0, widthC - 1);
        var random_yC = getRandomInt(0, heightC - 1);
        var random_xD = getRandomInt(0, widthD - 1);
        var random_yD = getRandomInt(0, heightD - 1);

        var sample_colorA = imgA.colorAt(random_xA, random_yA);
        var sample_colorB = imgB.colorAt(random_xB, random_yB);
        var sample_colorC = imgC.colorAt(random_xC, random_yC);
        var sample_colorD = imgD.colorAt(random_xD, random_yD);
        if (sample_color == "00cdcd") {

            var screen_x = random_x + x;
            var screen_y = random_y + y;

            console.log("Found a Target at: " + screen_x + ", " + screen_y + " color " + sample_color);
            return { x: screen_x, y: screen_y };

            /* if (confirmTarget(screen_x, screen_y)) {
                console.log("Found a Target at: " + screen_x + ", " + screen_y + " color " + sample_color);
                return { x: screen_x, y: screen_y };
            } else {
                console.log("Unconfirmed Target at: " + screen_x + ", " + screen_y + " color " + sample_color);
                sleep(getRandomInt(7000, 10000));
            } */
        }
/*         console.log("Found a  FALSE Target at: " + screen_x + ", " + screen_y + " color " + sample_color);
 */
    }
    return false;
}

function confirmTarget(screen_x, screen_y) {

    robot.moveMouseSmooth(screen_x, screen_y, 1);
    sleep(300);

    var x = screen_x + 145,
        y = screen_y + 55,
        width = 57,
        height = 20;
    var img = robot.screen.capture(x, y, width, height);

    var check_colors = ["00ffff", "0ecac8", "00f9fb", "00ebec", "0af4f2", "00feff", "17dad6", "20cbc6", "00fbfc", "00f8f9", "02ffff", "00d2d2", "1ecec7", "0fc3c2", "0bdcdb", "19ceca"];

    for (var i = 0; i < 500; i++) {
        var random_x = getRandomInt(0, width - 1);
        var random_y = getRandomInt(0, height - 1);
        var sample_color = img.colorAt(random_x, random_y);

        if (check_colors.includes(sample_color)) {
            console.log('Confirmado exitosamente.');
            return true;
        }
    }

    return false;
}

function sleep(ms) {
    Atomics.wait(new Int32Array(new SharedArrayBuffer(4)), 0, 0, ms);
}

function getRandomInt(min, max) {
    min = Math.ceil(min);
    max = Math.floor(max);
    return Math.floor(Math.random() * (max - min + 1)) + min;
}


function main() {
    console.log('Empezando...');
    sleep(4000);
    outer_loop:
        while (true) {
            if (fullInventory()) {
                console.log("Vaciando Inventario");
                dropInventory();
            }
            var target = findTarget();
            if (target == false) {
                console.log("No se encontró")
                continue outer_loop;
            }
            robot.moveMouseSmooth(target.x, target.y, 1);
            robot.mouseClick();
            sleep(getRandomInt(5000, 10000));
            while (isAnimating()) {
                if (fullInventory()) {
                    console.log("Vaciando Inventario");
                    dropInventory();
                    sleep(getRandomInt(1000, 2000));
                } else {
                    sleep(getRandomInt(1000, 2000));
                }
                sleep(getRandomInt(1000, 2000));
            }
        }
}

function isAnimating() {
    var x = 94,
        y = 86,
        width = 172,
        height = 31;
    var img = robot.screen.capture(x, y, width, height);
    var check_colors = ["00f500", "00d200", "00d900", "0ee007", "00ff00", "10d208", "01ff00", "01ff01"];
    for (var i = 0; i < 500; i++) {
        var random_x = getRandomInt(0, width - 1);
        var random_y = getRandomInt(0, height - 1);
        var sample_color = img.colorAt(random_x, random_y);

        if (check_colors.includes(sample_color)) {
            return true;
        }
    }
    return false;
}

function fullInventory() {
    var inventory_x = 1732;
    var inventory_y = 915;
    var pixel_color = robot.getPixelColor(inventory_x, inventory_y);
    return !(pixel_color == "494035")
}
main();